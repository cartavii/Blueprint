#include "Blueprint/Core/Sign.hpp"

float Blueprint::Core::signF(const float x) {
    return sign(x);
}

double Blueprint::Core::signD(const double x) {
    return sign(x);
}

int Blueprint::Core::signI(const float x) {
    return sign(x);
}
